# Lab1_ENGCE301 – Dev Environment Setup & First Web Page  
วิชา: ENGCE301 – Software Design and Development  
สัปดาห์ที่: 1  
เวลา: 3 ชั่วโมง (ปฏิบัติ)

---

## 1. ข้อมูลทั่วไปของ Lab

**ชื่อ Lab:**  
Dev Environment Setup & “Hello ENGCE301” Web Page

**จุดประสงค์หลัก**

เมื่อทำ Lab นี้เสร็จ นักศึกษาจะสามารถ:

1. ติดตั้งและตรวจสอบการทำงานของเครื่องมือพื้นฐานสำหรับการพัฒนาเว็บ
   - Git  
   - Visual Studio Code (VS Code)  
   - Node.js + npm  
   - Browser DevTools (Chrome/Edge/Firefox)
2. สร้างโครงโปรเจกต์สำหรับวิชา ENGCE301 (โฟลเดอร์กลุ่ม + Git repository)
3. สร้างหน้าเว็บ HTML อย่างง่ายและเปิดดูผ่าน Browser
4. ใช้ Git ทำงานพื้นฐาน: `init`, `status`, `add`, `commit`, (และ `push` ขึ้น remote ถ้ามี)
5. ใช้ Browser DevTools ดูโครงสร้าง DOM และ Console ได้ในระดับพื้นฐาน

**เชื่อมกับ CLO รายวิชา**

- **CLO1** เข้าใจพื้นฐานกระบวนการพัฒนาซอฟต์แวร์ (SDLC, tooling)  
- **CLO4** เริ่มต้นพัฒนาและตั้งค่า environment ได้  
- **CLO6** ทำงานเป็นทีมและใช้เครื่องมือร่วมกันได้

---

## 2. โครงสร้าง Lab (ภาพรวม)

Lab นี้แบ่งเป็น 4 ส่วนหลัก:

1. **Part A – Environment Check & Install**  
   ตรวจสอบ/ติดตั้ง Git, Node.js, VS Code, Browser DevTools
2. **Part B – Project Folder & HTML “Hello ENGCE301”**  
   สร้างโฟลเดอร์โปรเจกต์, สร้างไฟล์ `index.html`
3. **Part C – Git Repository & Basic Workflow**  
   สร้าง Git repo, ทำ commit ครั้งแรก, (เชื่อม remote ถ้ามี)
4. **Part D – DevTools & Mini Reflection**  
   เปิดเว็บด้วย browser, ใช้ DevTools, ตอบคำถามสั้น ๆ

---

## 3. การเตรียมตัวก่อนเข้า Lab (Pre-lab)

ก่อนเริ่มลงมือ แนะนำให้:

1. เตรียม **บัญชี email** ที่ใช้สมัคร GitHub/GitLab ได้ (เช่น Gmail ของมหาวิทยาลัย)
2. ถ้าในห้อง Lab ใช้เครื่องส่วนตัว:
   - ตรวจสอบว่ามีสิทธิ์ติดตั้งโปรแกรม (Admin/Password)
3. แนะนำ Browser:
   - Google Chrome หรือ Microsoft Edge (DevTools ใช้ง่าย)

> ถ้าใช้เครื่อง Lab ของคณะ: ให้อาจารย์/TA แจ้งเวอร์ชันเครื่องมือที่ติดตั้งไว้แล้ว หรือติดตั้งล่วงหน้าให้พร้อม

---

## 4. Part A – Environment Check & Install (ประมาณ 60 นาที)

> เป้าหมาย: ให้ทุกคนมี Git, Node.js, VS Code และรู้จักเปิด Browser DevTools

### 4.1 ตรวจสอบ Git

เปิด **Terminal** ตามระบบปฏิบัติการ:

- Windows:  
  - ใช้ **Command Prompt**, **PowerShell**, หรือ **Git Bash**
- macOS / Linux:  
  - ใช้ **Terminal**

พิมพ์คำสั่ง:

```bash
git --version
```

ถ้าขึ้นประมาณ:

```text
git version 2.xx.x
```

แสดงว่ามี Git แล้ว → ข้ามไปข้อ 4.2 ได้

ถ้า **ไม่พบ** (เช่น `command not found` หรือ `git is not recognized`):

* **Windows**

  1. เปิด browser เข้าเว็บ `https://git-scm.com/`
  2. ดาวน์โหลด *Git for Windows*
  3. ติดตั้งด้วยค่ามาตรฐาน (Next → Next → Finish)
  4. ปิด/เปิด Command Prompt ใหม่ แล้วลอง `git --version` อีกครั้ง

* **macOS (ใช้ Homebrew)**

  ```bash
  # ถ้ามี brew แล้ว
  brew install git
  ```

* **Ubuntu / Debian**

  ```bash
  sudo apt update
  sudo apt install git
  git --version
  ```

> ให้นักศึกษาบันทึก screenshot หรือจดเวอร์ชัน Git ลงในใบงาน Lab

---

### 4.2 ตั้งค่า Git เบื้องต้น (ชื่อและอีเมล)

ใน Terminal (เครื่องใครเครื่องมัน):

```bash
git config --global user.name "ชื่อจริง ภาษาอังกฤษหรือไทยก็ได้"
git config --global user.email "your_email@example.com"
```

ตรวจสอบค่า:

```bash
git config --global user.name
git config --global user.email
```

จดค่าในใบงาน Lab (จะใช้เพื่อ debug เวลา commit)

---

### 4.3 ตรวจสอบ Node.js และ npm

บน Terminal เดิม:

```bash
node -v
npm -v
```

ถ้ามี Node.js แล้ว จะเห็น:

```text
v18.xx.x   (หรือ LTS ใหม่กว่า)
8.xx.x     (npm)
```

ถ้าไม่มี:

* เข้าเว็บ `https://nodejs.org/`
* ดาวน์โหลด **LTS version**
* ติดตั้งด้วยค่ามาตรฐาน (Next → Next → Finish)
* ปิด/เปิด Terminal ใหม่ แล้วลอง `node -v`, `npm -v` อีกครั้ง

> เหตุผลที่ใช้ LTS: เสถียรกว่าและส่วนใหญ่ library รองรับดี

---

### 4.4 ตรวจสอบ Visual Studio Code (VS Code)

1. ถ้ามีแล้ว:

   * เปิด VS Code
   * ไปที่เมนู `Help → About` ดูเวอร์ชันและจดในใบงาน
2. ถ้ายังไม่มี:

   * เข้าเว็บ `https://code.visualstudio.com/`
   * ดาวน์โหลด VS Code ตามระบบปฏิบัติการ
   * ติดตั้งตามขั้นตอน (ใช้ค่ามาตรฐานได้)

**เช็ค Extension แนะนำ (ยังไม่ต้องลงครบใน Lab 1 ก็ได้):**

* Live Server
* Prettier – Code formatter
* (ภายหลัง: ESLint, GitLens ฯลฯ)

---

### 4.5 ตรวจสอบ Browser DevTools

1. เปิด **Chrome/Edge/Firefox**
2. เปิดเว็บอะไรก็ได้ (เช่น google.com)
3. กด:

   * Windows/Linux: `F12` หรือ `Ctrl + Shift + I`
   * macOS: `Cmd + Option + I`

ควรเห็น panel DevTools มี tab เช่น **Elements**, **Console**, **Network**

ให้ตอบในใบงาน:

* Browser ที่ใช้: (Chrome / Edge / Firefox)
* Tab ที่คิดว่าจะใช้บ่อยมากที่สุดในการพัฒนาเว็บ (เดาได้เลย)

---

## 5. Part B – Project Folder & HTML “Hello ENGCE301” (≈ 60 นาที)

> เป้าหมาย: ให้ทุกกลุ่มมีโฟลเดอร์โปรเจกต์ + ไฟล์ HTML แรก

### 5.1 สร้างโฟลเดอร์โปรเจกต์กลุ่ม

ให้ **เลือกคนหนึ่งในกลุ่ม** เป็น “คนถือ repo หลัก” (คนอื่นจะ clone ในภายหลัง)

ตัวอย่างโครง:

```text
ENGCE301/
  term-project/
    group-01/
    group-02/
    ...
```

บนเครื่องของหัวหน้ากลุ่ม (หรือคนกลาง):

**Windows (PowerShell ตัวอย่าง)**

```powershell
# ไปยังโฟลเดอร์ที่ต้องการเก็บงาน (เช่น Documents)
cd $HOME\Documents

# สร้างโฟลเดอร์หลักวิชา
mkdir ENGCE301
cd ENGCE301

# สร้างโฟลเดอร์ term-project และ group
mkdir term-project
cd term-project
mkdir group-01
cd group-01
```

**macOS / Linux (Terminal)**

```bash
cd ~
mkdir -p ENGCE301/term-project/group-01
cd ENGCE301/term-project/group-01
```

> แทน `group-01` ด้วยเลขกลุ่มจริง เช่น group-03, group-07

---

### 5.2 เปิดโฟลเดอร์ด้วย VS Code

ใน Terminal (อยู่ใน `group-XX` แล้ว):

```bash
code .
```

ถ้าใช้ไม่สำเร็จ (เช่นเครื่อง Lab ยังไม่ได้ตั้ง `code` command) ให้:

* เปิด VS Code → `File → Open Folder…` → เลือกโฟลเดอร์ `group-XX`

---

### 5.3 สร้างไฟล์ `index.html` แรก

ใน VS Code:

1. คลิกขวาใน Explorer → New File
2. ตั้งชื่อไฟล์: `index.html`
3. ใส่เนื้อหาเริ่มต้น:

```html
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>Hello ENGCE301 - Group XX</title>
</head>
<body>
  <h1>Hello ENGCE301 👋</h1>
  <h2>กลุ่ม: Group XX</h2>

  <p>สมาชิกในกลุ่ม:</p>
  <ul>
    <li>6501xxxx นาย A</li>
    <li>6501xxxx นางสาว B</li>
    <li>6501xxxx นาย C</li>
    <!-- เพิ่มตามจำนวนสมาชิกจริง -->
  </ul>

  <hr />

  <p>
    ในสัปดาห์ที่ 1 เราเรียนเรื่อง
    <strong>Software Engineering, SDLC, Waterfall vs Agile</strong>
    และการเตรียม Dev Environment
  </p>
</body>
</html>
```

> ให้แก้ `Group XX` เป็นเลขกลุ่มจริง และใส่รายชื่อสมาชิกให้ครบ

---

### 5.4 ทดสอบเปิดหน้าเว็บใน Browser

**วิธีง่ายที่สุด (ดับเบิลคลิก):**

1. ใน File Explorer (Windows) / Finder (macOS) เปิดโฟลเดอร์ `group-XX`
2. ดับเบิลคลิกที่ `index.html` → Browser จะเปิดหน้าเว็บขึ้นมา

**วิธีใช้ Live Server (ถ้ามี extension):**

1. ใน VS Code เปิด `index.html`
2. คลิกขวาใน editor → เลือก **Open with Live Server**
3. Browser จะเปิดที่ URL ประมาณ `http://127.0.0.1:5500/index.html`

---

## 6. Part C – Git Repository & Basic Workflow (≈ 60 นาที)

> เป้าหมาย: ให้กลุ่มมี Git repository สำหรับ Term Project และมี commit แรก

### 6.1 เริ่มต้น Git Repository

ตรวจสอบว่า Terminal อยู่ในโฟลเดอร์ `group-XX`:

```bash
pwd      # macOS/Linux
cd       # PowerShell ใช้ 'pwd' ได้เหมือนกัน หรือดู path ด้านบน
```

ถ้า path ลงท้ายด้วย `.../ENGCE301/term-project/group-XX` ให้พิมพ์:

```bash
git init
```

จะเห็นข้อความประมาณ:

```text
Initialized empty Git repository in .../group-XX/.git/
```

---

### 6.2 ตรวจสอบสถานะไฟล์ (git status)

```bash
git status
```

หากทุกอย่างถูกต้องจะเห็น:

```text
Untracked files:
  (use "git add <file>..." to include in what will be committed)
        index.html
```

แปลว่า Git เห็น `index.html` แต่ยังไม่ได้ tracked

---

### 6.3 เพิ่มไฟล์เข้า staging area

ใช้:

```bash
git add index.html
```

หรือจะเพิ่มทุกไฟล์ในโฟลเดอร์:

```bash
git add .
```

แล้วตรวจสอบอีกครั้ง:

```bash
git status
```

ควรเห็น:

```text
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
        new file:   index.html
```

---

### 6.4 ทำ commit แรก

**ตั้งค่า user (ถ้ายังไม่ได้ตั้ง global หรืออยาก override):**

```bash
git config user.name "ชื่อของคุณ"
git config user.email "your_email@example.com"
```

จากนั้น commit:

```bash
git commit -m "Initial commit: Hello ENGCE301 page"
```

ถ้าเรียบร้อยแล้ว:

```bash
git log --oneline
```

จะเห็น commit แรก เช่น:

```text
a1b2c3d Initial commit: Hello ENGCE301 page
```

> ให้หัวหน้ากลุ่มบันทึก screenshot ของ `git log --oneline` เพื่อแนบในการส่งงาน

---

### 6.5 สร้าง `.gitignore` (แนะนำสำหรับอนาคต)

สร้างไฟล์ `.gitignore` ในโฟลเดอร์ `group-XX`:

```text
node_modules/
dist/
.env
```

จากนั้น:

```bash
git add .gitignore
git commit -m "Add basic .gitignore"
```

---

### 6.6 เชื่อมกับ Remote Repository (ถ้ามี GitHub/GitLab พร้อม)

> ถ้ายังไม่มีบัญชี/ความพร้อม Remote สามารถข้ามส่วนนี้ชั่วคราว แล้วให้ทำเป็นการบ้านต่อ

**ขั้นตอนภาพรวม:**

1. ไปที่ GitHub/GitLab → สร้าง repository ใหม่ ชื่อเช่น `engce301-group-XX`

   * อย่าให้สร้าง README อัตโนมัติ (เลือก empty repo)
2. ระบบจะให้คำสั่งประมาณนี้ (ตัวอย่าง GitHub HTTPS):

```bash
git remote add origin https://github.com/USERNAME/engce301-group-XX.git
git branch -M main
git push -u origin main
```

3. กลับมาที่ Terminal (ใน `group-XX`) แล้วรันคำสั่งด้านบน (แทน URL ตามของจริง)
4. เปิดหน้าเว็บ repository ตรวจดูว่ามีไฟล์ `index.html` และ `.gitignore` แล้ว

---

## 7. Part D – DevTools & Mini Reflection (≈ 30–45 นาที)

> เป้าหมาย: ให้นักศึกษารู้จักใช้ DevTools ดู DOM และ Console และเชื่อมกับแนวคิด SDLC/Agile

### 7.1 ใช้ Elements Panel

1. เปิด Browser → เปิดหน้า `index.html` ของกลุ่ม
2. เปิด DevTools (`F12` หรือ `Ctrl+Shift+I` / `Cmd+Option+I`)
3. เลือก tab **Elements**
4. ลองคลิก `<h1>` หรือ `<h2>` แล้วแก้ข้อความชั่วคราว เช่น
   เปลี่ยน “Hello ENGCE301 👋” เป็น “Hello Software Engineering 👩‍💻👨‍💻”
5. สังเกตว่าข้อความในหน้าเว็บเปลี่ยนทันที

   * อธิบายในใบงาน: “ทำไมพอ Refresh แล้วข้อความเปลี่ยนกลับ?”

---

### 7.2 ใช้ Console Panel

1. เลือก tab **Console**
2. ลองพิมพ์ใน Console:

```javascript
console.log("ENGCE301 Lab1 - DevTools Test");
```

3. กด Enter → ดู log ที่แสดง
4. เพิ่ม `<script>` เล็ก ๆ ใน `index.html`:

```html
<script>
  console.log("Hello from Group XX");
</script>
```

5. บันทึกไฟล์ → Refresh หน้าเว็บ → ดูว่า Console แสดงข้อความอะไร

ให้ตอบในใบงาน:

* Console มีประโยชน์อย่างไรเวลา debug โค้ด JavaScript?

---

### 7.3 Mini Reflection (เขียนสั้น ๆ)

ให้แต่ละคนเขียนตอบคำถามลงในใบงานหรือ Google Form (3–5 บรรทัดต่อข้อ):

1. ในมุมมองคุณ “Programming” ต่างจาก “Software Engineering” อย่างน้อย 2 ข้ออย่างไร?
2. ใน Term Project ถ้าต้องเลือกใช้ Waterfall หรือ Agile (Scrum/Kanban อย่างง่าย) คุณคิดว่าทีมคุณน่าจะเหมาะกับแบบไหน? เพราะอะไร?

---

## 8. สิ่งที่ต้องส่ง (Submission Requirements)

**ส่งเป็นรายกลุ่ม** (1 ชุดต่อ 1 กลุ่ม):

1. **URL ของ Git Repository** (ถ้ามี remote)

   * ถ้าไม่มี remote ให้ส่ง zip ของโฟลเดอร์ `group-XX` พร้อม `.git` (หรือส่งเฉพาะไฟล์สำคัญตามที่อาจารย์กำหนด)
2. **โครงสร้างไฟล์ใน repo อย่างน้อย:**

   * `index.html`
   * `.gitignore`
   * *(ถ้าอาจารย์กำหนดเพิ่ม: `README.md` สรุปแผน Waterfall vs Agile แบบย่อ)*
3. **Screenshots**

   * `git log --oneline` ที่แสดง commit อย่างน้อย 2 ครั้ง
   * หน้าเว็บ `index.html` ใน Browser
   * DevTools – Elements และ Console (เห็น log ตามที่ทดลอง)
4. **ใบงาน Lab (รูปแบบที่อาจารย์กำหนด)**

   * คำตอบ Environment Check (เวอร์ชัน Git/Node/Browser)
   * คำถาม DevTools
   * Mini Reflection

---

## 9. Rubric แนะนำ (20 คะแนน)

> อาจารย์สามารถปรับน้ำหนักคะแนนตามนโยบายรายวิชา

| รายการประเมิน                                         | คะแนน  |
| ----------------------------------------------------- | ------ |
| Environment Setup ครบ (Git/Node/VS Code/Browser)      | 4      |
| โครงโปรเจกต์ + `index.html` ถูกต้อง / ครบ             | 4      |
| ใช้ Git พื้นฐานได้จริง (init, add, commit, log)       | 4      |
| DevTools: ใช้ Elements + Console ตามใบงาน (มีหลักฐาน) | 3      |
| ใบงาน + Mini Reflection (เข้าใจแนวคิด SE/SDLC)        | 3      |
| ความเรียบร้อยและการทำงานเป็นทีม                       | 2      |
| **รวม**                                               | **20** |

---

## 10. สรุป Lab

หลังจากทำ Lab1 นักศึกษาควร:

* มี **Dev Environment พร้อมใช้** สำหรับสัปดาห์ถัดไป (HTML/CSS/JS, Node, Git)
* มี **Git repo กลุ่ม** ที่จะใช้ต่อเนื่องทั้งเทอมสำหรับ Term Project
* เริ่มเปลี่ยน mindset จาก “แค่เขียนโปรแกรมให้รันได้” → “วางโครงเพื่อทำงานเป็นทีมและพัฒนาต่อ”
* มีประสบการณ์แรกในการใช้ DevTools เพื่อ debug หน้าเว็บ

> Lab 1 คือฐานสำคัญของวิชา ENGCE301 – ถ้าเครื่องมือพร้อม ทีมพร้อม เทอมที่เหลือจะลื่นขึ้นมาก 🙂
