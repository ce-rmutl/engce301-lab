# ENGCE301 – Week 4 LAB  
## HTML, CSS, Responsive Design + UX Thinking

> โจทย์นี้ออกแบบมาให้ทำ **ในห้อง 3 ชม.** และ **ทำต่อที่บ้าน**  
> เป้าหมายคือให้นศ.ได้ฝึกทั้ง **เขียนโค้ดเว็บ** และ **คิดจากมุมมองผู้ใช้ (UX)**

---

## 0. เป้าหมายสำคัญของ LAB สัปดาห์นี้ 🎯

หลังจบ LAB นี้ นักศึกษาควรจะ…

1. สร้างหน้าเว็บด้วย HTML5 + Semantic Tags ได้เอง  
2. ออกแบบหน้าด้วย CSS ให้ **สวย / อ่านง่าย / ใช้สีและฟอนต์ได้เหมาะสม**  
3. ใช้ **Flexbox + (ตัวเลือกเสริม) CSS Grid** เพื่อจัด layout ที่เป็นระบบ  
4. ทำหน้าเว็บให้ **Responsive** รองรับมือถือ แท็บเล็ต และ Desktop  
5. ฝึกคิดแบบ **User-Centred Design**  
   - สร้าง Personas ง่าย ๆ  
   - ออกแบบ layout ตามความต้องการของ persona  
6. ใช้ **Git + GitHub + GitHub Pages** เพื่อส่งงานและ deploy เว็บจริง

---

## 1. เตรียมตัวก่อนเริ่ม LAB 🛠️

### เครื่องมือที่ต้องมี

- VS Code  
- Git (ติดตั้งจาก [git-scm.com](https://git-scm.com))  
- GitHub Account  
- Live Server Extension บน VS Code

### ตั้งค่า Git ครั้งแรก (ถ้ายังไม่เคย)

```bash
git config --global user.name "ชื่อของคุณ"
git config --global user.email "อีเมลของคุณ"
```

---

## 2. ภาพรวมงาน LAB – “Mini Profile Site สำหรับ Persona ของคุณ”

ให้นักศึกษาออกแบบและพัฒนาเว็บเพจ 1–3 หน้า
ในธีม **“Mini Profile / Portfolio / Dashboard”** สำหรับ *ผู้ใช้คนหนึ่ง* (Persona)
เช่น

* นักศึกษาปี 1 ที่ต้องการติดตามงานที่ต้องส่ง
* เจ้าของร้านกาแฟเล็ก ๆ ที่ต้องการดูคิวออเดอร์
* Tutor สอนพิเศษที่ต้องทำตารางสอน และแสดงรีวิว

> **ไม่จำเป็นต้องเป็น “ตัวเราเอง”** แต่ต้องคิดว่า…
> “ถ้าเราทำเว็บให้ persona นี้จริง ๆ เขาอยากได้หน้าตาแบบไหน?”

---

## 3. Part A – UX Mini Workshop (ทำในห้อง 30–40 นาที)

### A1. สร้าง Persona (คนละ 1 คน)

ในกระดาษ/Notion/Google Doc ให้ตอบคำถามต่อไปนี้:

1. **ชื่อ (สมมติ), อายุ, สถานะ**
2. **เป้าหมาย (Goals)** – เขาต้องการทำอะไรให้สำเร็จด้วยเว็บ/ระบบนี้ 3 ข้อ
3. **Pain Points** – ปัญหาหรือความหงุดหงิด 3 ข้อ
4. **พฤติกรรม** – ใช้มือถือ/คอมเยอะไหม? เวลาไหน?
5. **Quote สั้น ๆ** สะท้อนตัวตน เช่น

   > “ผมไม่มีเวลามาก อยากเห็นสรุปทุกอย่างในหน้าเดียว”

### A2. วาด User Journey แบบคร่าว ๆ (5–7 ขั้นตอน)

* เริ่มจาก “นึกได้ว่าต้องใช้เว็บนี้” → “ใช้เสร็จแล้ว”
* ระบุ **แต่ละขั้น** ว่า persona ทำอะไร และรู้สึกอย่างไร (ดี/แย่)

> จุดที่ persona *รู้สึกแย่* คือโอกาสที่เราจะออกแบบ UI/UX ให้ดีขึ้น

---

## 4. Part B – โครง HTML (ทำในห้อง 45–60 นาที)

สร้างโปรเจ็กต์ใหม่ชื่อ `week4-ux-responsive-site`

```text
week4-ux-responsive-site/
  ├─ images/
  ├─ index.html
  └─ style.css   (สร้างทีหลัง)
```

### B1. สร้างโครง HTML5

ใน `index.html` ให้เขียนโครงพื้นฐานเอง (ห้ามก็อปทั้งหมดจากตัวอย่าง) โดยมีอย่างน้อย:

* `<header>`
* `<nav>`
* `<main>`
* `<section>` 2–4 ส่วน
* `<footer>`

> **แนะนำ Section ตัวอย่าง** (ปรับตามโจทย์ของกลุ่มได้)

* Hero / Overview ของ persona หรือระบบ
* Tasks / Features สำคัญ (Persona ใช้ทำอะไร)
* ตาราง/รายการสรุปข้อมูล (To-Do, ตารางเรียน, เมนู ฯลฯ)
* Contact / Call-to-Action

**โครงตัวอย่างแบบสั้น (ใช้เป็นไอเดียเท่านั้น)**

```html
<body>
  <header>
    <h1>Dashboard สำหรับ [ชื่อ Persona]</h1>
    <nav>
      <!-- TODO: เพิ่มลิงก์ anchor เช่น #today, #tasks, #history -->
    </nav>
  </header>

  <main>
    <section id="today">
      <h2>ภาพรวมวันนี้</h2>
      <!-- TODO: ข้อมูลสรุป 2–3 เรื่องที่ persona สนใจมากที่สุด -->
    </section>

    <section id="tasks">
      <h2>งานที่ต้องทำ</h2>
      <!-- TODO: ใช้ <ul> หรือ <table> แสดงรายการ -->
    </section>

    <section id="extra">
      <h2>ข้อมูลอื่น ๆ</h2>
      <!-- TODO: ขึ้นอยู่กับ persona ของคุณ -->
    </section>
  </main>

  <footer>
    <p>สร้างเพื่อ LAB Week 4 – ENGCE301</p>
  </footer>
</body>
```

### B2. กติกา

* ห้ามปล่อย `<div>` ลอย ๆ โดยไม่จำเป็น – ให้ใช้ **semantic tags** ให้มากที่สุด
* เนื้อหาต้อง **อิงจาก Persona + Journey** ที่คิดไว้
* ทุกคนใส่ **คอมเมนต์ TODO** ในโค้ดให้ชัดเจน ว่าตรงไหนยังจะพัฒนาเพิ่มทีหลัง

---

## 5. Part C – ตกแต่งด้วย CSS + Flexbox (ทำในห้อง 1–1.5 ชม.)

สร้างไฟล์ `style.css` และเชื่อมใน `<head>`:

```html
<link rel="stylesheet" href="style.css">
```

### C1. Base Style (นักศึกษาต้องเขียนเองตามแนวคิดต่อไปนี้)

**ให้เขียนเองจากแนวทาง ไม่แจกโค้ดเต็ม:**

* Reset เบื้องต้น: `margin: 0; padding: 0; box-sizing: border-box;`
* กำหนด `body`:

  * ฟอนต์ (font-family)
  * สีพื้นหลัง
  * สีตัวอักษร
  * `line-height` ที่อ่านง่าย

### C2. Header + Navigation

* ใช้ **gradient** หรือสีพื้นให้ header เด่น
* ทำเมนู `<nav>` เป็นแถวแนวนอนด้วย **Flexbox**

  * จัด `justify-content: center` หรือ `space-between`
  * ทำ `:hover` ให้เห็นว่าปุ่มถูกชี้อยู่

> ❗ **โจทย์เพิ่ม:**
> ออกแบบให้เมนูของคุณ *สะท้อน persona* เช่น
>
> * ถ้า persona รีบ → เมนูไม่ควรเยอะ
> * ถ้า persona ต้องดูข้อมูลหลายมุมมอง → อาจมี dropdown/แท็บเพิ่ม (ในเวอร์ชันถัดไป)

### C3. Main Layout ด้วย Flexbox / Grid

1. เลือก section หนึ่งที่สำคัญที่สุด เช่น `#today` หรือ `#tasks`
2. ใช้ **Flexbox** หรือ **CSS Grid** เพื่อจัด layout ภายใน

   * ตัวอย่าง: card 3 ใบเรียงกัน, หรือสองคอลัมน์ (ข้อมูล + สถิติ)

> ให้ใส่คอมเมนต์ใน CSS ว่า
> `/* ใช้ Flexbox เพราะ...? */` หรือ `/* ใช้ Grid เพราะ...? */`

### C4. Visual Design เพิ่มเติม (ให้นักศึกษาออกแบบเอง)

* ใส่ `box-shadow` บางส่วน
* ใช้ `border-radius`
* ใช้ **สี 3–4 สีหลัก** (ไม่ให้รกเกินไป)
* กำหนด style ของ `<h1>, <h2>, <p>, <li>` ให้ชัดเจนและอ่านง่าย

---

## 6. Part D – Responsive Design (ทำช่วงท้าย + ต่อที่บ้าน)

ใช้ไฟล์ `style.css` เดิม เพิ่มส่วน responsive เข้าไป

### D1. Base Responsive

* กำหนด `img { max-width: 100%; height: auto; }`
* `main` ควรมี `max-width` และ `padding` ที่สมเหตุสมผล

### D2. Media Queries (อย่างน้อย 2 ช่วง)

**แนะนำ:**

```css
/* Tablet (<= 768px) */
@media (max-width: 768px) {
  /* TODO: ลด padding, ทำ card เป็น 2 คอลัมน์ → 1 คอลัมน์ ฯลฯ */
}

/* Mobile (<= 480px) */
@media (max-width: 480px) {
  /* TODO: ให้ nav เรียงแนวตั้ง, ลดฟอนต์, card เต็มความกว้าง */
}
```

ให้นักศึกษาตัดสินใจเองว่า…

* ส่วนไหนควรเปลี่ยน layout ก่อน (สำคัญสุดสำหรับมือถือ)
* เมนูแบบไหนที่ persona ใช้ง่ายที่สุดบนจอเล็ก

> **คำถามสะท้อนคิด (ใส่คำตอบสั้น ๆ ไว้ท้ายไฟล์ `README.md`)**
>
> 1. บนมือถือ persona ของคุณอยากเห็น “ข้อมูลอะไรเป็นอันดับแรก”?
> 2. คุณเลือกจะซ่อน/ย้ายข้อมูลอะไรไปไว้ด้านล่าง?

---

## 7. Part E – Refactor & Code Smell (ทำต่อที่บ้าน)

ให้เปิดโค้ด `index.html` และ `style.css` ของตนเอง แล้วตรวจ:

1. มี “**Magic numbers**” ใน CSS ไหม? (เช่น margin 37px, font-size 13px แบบแปลก ๆ)

   * ถ้ามี ให้ปรับเป็นค่าที่มีเหตุผล เช่น 1rem, 1.5rem หรือ 32px เป็นต้น
2. มี **โค้ดซ้ำ ๆ** ไหม? (เช่น style ของ card เหมือนกันหลายที่)

   * สร้าง class `.card`, `.btn-primary` ฯลฯ แล้วนำไปใช้ซ้ำ
3. แยกส่วน CSS เป็นหมวด เช่น

   * `/* Layout */`, `/* Components */`, `/* Typography */`
   * ใส่คอมเมนต์หัวข้อให้ชัด

เขียนสรุปสั้น ๆ 5–10 บรรทัดใน `README.md` ว่า

* คุณเจอ code smell อะไรในงานของตัวเองบ้าง
* คุณแก้ด้วยวิธีใด (อ้างถึงแนวคิดจากในคลาส: SRP, DRY, Magic Number ฯลฯ)

---

## 8. Part F – Git, GitHub และ GitHub Pages 📤

> ถ้าเคยใช้แล้ว สามารถ reuse ความรู้เดิม แต่ต้องมี commit ใหม่สำหรับ Lab Week 4

1. สร้าง **GitHub Repository** ชื่อ `engce301-week4-ux-responsive`
2. เพิ่มไฟล์:

   * `index.html`
   * `style.css`
   * `README.md` (ใส่คำตอบ reflection สั้น ๆ)
3. Commit พร้อมข้อความสื่อความหมาย เช่น
   `git commit -m "Add responsive layout for persona dashboard"`
4. Push ขึ้น GitHub
5. เปิดใช้ **GitHub Pages** แล้วทดสอบว่าดูได้บนมือถือจริง
6. ส่ง:

   * URL ของ Repo
   * URL ของ GitHub Pages
   * Screenshot บนมือถือ + Desktop

---

## 9. เกณฑ์การให้คะแนน 

### 1) UX & Content (20 คะแนน)

* Persona + Journey มีเหตุผล อิงกับหน้าเว็บจริง (10)
* เนื้อหาในหน้าเว็บสอดคล้องกับความต้องการของ persona (10)

### 2) HTML & Structure (20 คะแนน)

* Semantic HTML ถูกต้อง, โครงสร้างชัดเจน (10)
* ใช้ id / class มีความหมาย, ไม่มี div เกินจำเป็น (10)

### 3) CSS & Visual Design (25 คะแนน)

* ฟอนต์ / สี / spacing อ่านง่าย, เป็นระบบ (10)
* ใช้ Flexbox / Grid ถูกต้อง และ layout ดูเป็นมืออาชีพ (10)
* มี effects เล็กน้อย (hover, shadow ฯลฯ) อย่างเหมาะสม (5)

### 4) Responsive & Refactor (25 คะแนน)

* หน้าเว็บใช้งานได้ดีบนมือถือและแท็บเล็ต (10)
* Media Query ทำงานจริง (10)
* มีการ refactor / แก้ code smell และอธิบายใน README (5)

### 5) Git & Deployment (10 คะแนน)

* ใช้ Git อย่างถูกต้อง (หลาย commit, message มีความหมาย) (5)
* GitHub Pages เปิดใช้และทดสอบได้จริง (5)

---

## 10. เคล็ดลับสำหรับนักศึกษา 💡

1. อย่ากลัวหน้า “โล่ง” – เว็บที่ดีคือเว็บที่อ่านง่าย
2. ดูตัวอย่าง dashboard / landing page จริง ๆ เพื่อหา inspiration
3. ใช้ DevTools ปรับ CSS สด ๆ แล้วค่อยกลับมาแก้ในไฟล์
4. Commit บ่อย ๆ เวลาเปลี่ยนอะไรใหญ่ ๆ
5. ถ้าทำเสร็จเร็ว ลองทำ “หน้าเพิ่ม” เช่น

   * หน้า History
   * หน้า Settings สำหรับ persona

**ขอให้สนุกกับการออกแบบเว็บที่เป็นของตัวเอง (และของ persona ของคุณ) จริง ๆ!**

